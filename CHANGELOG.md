# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-09-10

### 新增

- 导出新增 `author`、`publishedAt`、`likes`、`isReply` 字段（JSON 与 CSV 同步）
- 评论去重键改为「作者 + 时间 + 正文」，修复不同用户的相同内容被静默合并的问题
- 进度改为节流落盘：新增 50 条或间隔 15 秒写入一次，不再每轮序列化整个存储
- 本地存储写入失败或序列化后超过 4 MB 时，面板以红/橙文字显式提示，不再静默降级
- 新增解析器单元测试（25 项）与脱敏结构 fixture

### 修复

- 兜底分支（页面无 `ytd-comment-thread-renderer`）在主评论模式下未排除回复容器，现已修正
- 英文单个回复按钮文案 `1 reply` 此前无法识别（正则 `replies?` 只能匹配 `replie`/`replies`）
- `youtu.be/<id>` 页面此前无法识别视频 id，现已支持
- `#expand-content` 不再与回复按钮共用 `dataset.ytcMark` 标记命名空间

### 兼容性

- 1.0.0 已抓取的数据按原「仅正文」键继续工作（载入时自动标记为旧键方案），不会产生重复记录
- JSON 导出 `text` 仍为首字段；CSV 首列仍为 `text`，新增列追加在后

### 变更

- `@updateURL` / `@downloadURL` 改为 jsDelivr 镜像，受限网络下自动更新可用
- 版本号 `1.0.0` → `1.1.0`

## [1.0.0] - 2026-09-10

- 首个公开版本：整理自个人使用的原始脚本，去除 emoji 与调试输出，补齐脚本元信息，逻辑保持不变
